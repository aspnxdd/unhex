{
  "name": "unhex",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "turbo:build": "turbo run build",
    "package:compile": "pnpm --filter package run compile",
    "package:test": "pnpm --filter package run test",
    "package:publish": "pnpm --filter package run publish"
  },
  "dependencies": {},
  "devDependencies": {
    "turbo": "^1.5.3"
  }
}